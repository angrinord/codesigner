django>=6.0,<7
django-environ>=0.12
huey>=3.3           # background task queue (SqliteHuey; run via `manage.py run_huey`)

# Production server / static serving (container).
gunicorn>=23
whitenoise>=6.6

# HPO core. 2.4.1 is the first release carrying the fixes this project used to
# install from a git branch for — scikit-learn 1.9's random-forest import among
# them — so it is a plain requirement again. Building its pyrfr dependency needs
# swig, which has no wheel; see the Dockerfile.
smac>=2.4.1
hypershap>=0.0.6
pandas>=2.3
scikit-learn>=1.9
numpy>=2.4
plotly>=6              # analytics figures (web/charts.py)
ConfigSpace>=1.2       # hyperparameter search spaces (also pulled by smac)

# Development / CI
pytest>=8
pytest-django>=4.9
