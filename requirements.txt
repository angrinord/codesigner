django>=6.0,<7
django-environ>=0.12
huey>=3.3           # background task queue (SqliteHuey; run via `manage.py run_huey`)

# Production server / static serving (container).
gunicorn>=23
whitenoise>=6.6

# HPO core. The PyPI smac 2.4.0 release has a bug; install the fixed build from
# git (pinned). Building its pyrfr dependency needs swig — see the Dockerfile.
smac @ git+https://github.com/automl/SMAC3.git@620e2876fc79dc596c1adac959adde695e399d4a
hypershap>=0.0.6
pandas>=2.3
scikit-learn>=1.9
numpy>=2.4

# Development / CI
pytest>=8
pytest-django>=4.9
